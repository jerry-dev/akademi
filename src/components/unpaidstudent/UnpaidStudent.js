import React from 'react';
import styles from './unpaidstudent.module.css';

const UnpaidStudent =  (props) => {
    return (
        <section className={styles.unpaidStudent}></section>
    );
}

export default UnpaidStudent;